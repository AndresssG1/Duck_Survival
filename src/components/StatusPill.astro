---
interface Props { status: 'active' | 'expired' | 'unverified' }
const { status } = Astro.props;
const labels = { active: 'Activo', expired: 'Expirado', unverified: 'Por verificar' };
---
<span class={`status status-${status}`}>{labels[status]}</span>
