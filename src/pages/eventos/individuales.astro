---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import { gameEvents } from '../../data/events';
const events = gameEvents.filter((event) => event.audience === 'individual');
---

<BaseLayout title="Eventos individuales" description="Eventos temporales individuales de Duck Survival.">
  <PageHero eyebrow="Calendario temporal" title="Eventos individuales" description="Actividades con fecha limitada cuyo progreso pertenece a cada jugador." />
  <section class="section shell narrow"><a class="back-link" href="/eventos/">&lt;- Eventos y desafíos</a>{events.length === 0 && <div class="event-empty"><p class="eyebrow">Sin eventos registrados</p><h2>Esperando el próximo calendario individual.</h2><p>Publicaremos aquí sus reglas y recompensas cuando puedan verificarse dentro del juego.</p></div>}</section>
</BaseLayout>
